

export const headerData = (key) => {
    try {
        const data = key 
        
        data.forEach(msData => {
            const {title} = msData
            const {rendered:PageTitle} =  title
            
            if(PageTitle === 'Home') {
                
            }

        });


    } catch {
        
    }
}