// Import 
    // Load
        import {load} from '../../storage/load'

const nav = () => {
    // Variable
        // Page  
            const Page = load('site')
        // User
            const User = load('user')

}