// Import 

/**
 * SetTitle
 * @param {String} NewTitle 
 */

/**
 * @param {string and  Number} NewTitle
 * 
 * 
 * @example
 * ```
 * 
 * ```
 */

export const SetTitle = (NewTitle) => {
    document.title = NewTitle;
}